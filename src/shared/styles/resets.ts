import * as colors from "./colors";
import * as styles from ".";

export const link = styles.ap({
  textDecoration: "none",
  color: colors.black,
});

export const header = styles.ap({
  margin: 0,
  padding: 0,
});

export const paragraph = styles.ap({
  margin: 0,
});

export const button = styles.ap({
  padding: 0,
  border: 0,
  textTransform: "none",
  fontSize: "100%",
  background: 0,
});

export const ul = styles.ap({
  padding: 0,
  listStyleType: "none",
});

export const li = styles.ap({
  padding: 0,
});
